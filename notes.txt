**Release Notes for Ticket: [Missing Ticket Number]**
18 January 2026 at 09:19 pm IST

### New Features
*   Introduced new mathematical functions: `subtract` and `mult`, expanding the system's basic arithmetic capabilities.

### Improvements
*   Enhanced security and configuration flexibility by no longer hardcoding sensitive API keys directly in the codebase. This makes the system more secure and easier to manage across different environments.

### Technical Details
*   Implemented `dotenv` library to manage API keys and other sensitive configurations through environment variables.
*   Migrated the `GEMINI_API_KEY` from hardcoded values to environment variables, improving security and adhering to best practices for credentials management.
*   Added `subtract` and `mult` functions to `src/index.js`, expanding core utility functions.